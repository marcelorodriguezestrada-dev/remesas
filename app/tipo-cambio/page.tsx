import TableroTipoCambio from "@/components/TableroTipoCambio";

export default function TipoCambioPage() {
  return (
    <div className="flex min-h-screen flex-col items-center bg-zinc-50 px-4 py-16 font-sans dark:bg-black">
      <TableroTipoCambio />
    </div>
  );
}
